PackageService Repo

Run
```
gradlew PackageService:bootRun
```

publish by running
```
gradlew PackageService.Client:publish
```

Remember to set the `gradle.properties` file in `~/.gradle/` or `C:\Users\<USER>\.gradle`
```
weweloRepoUser=xxx
weweloRepoPassword=xxx
```
